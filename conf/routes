# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       App.Index
GET     /users                                  User.GetAll
GET     /users/:id                              User.GetUser

# favicon requests
GET     /favicon.ico                           Static.Serve("ext/images","favicon.png") 

# Map external resource from the /ext folder to the /ext path
GET     /ext/*filepath                         Static.Serve("ext")

# Catch all
#*       /{controller}/{action}                  {controller}.{action}
